# Secrets — NEVER commit API keys
.env
.env.local
.env.*.local
*.key
secrets/

# Dependencies (for when you add a backend later)
node_modules/
package-lock.json
yarn.lock
pnpm-lock.yaml

# Build output (for when you add tooling later)
dist/
build/
.next/
.nuxt/

# Editor / OS
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local notes — uncomment if you want a private scratchpad
# NOTES.local.md
